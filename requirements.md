## Number of sms sent
We need to change the setup to send only two sms per customer. one before passing deadline and one after passing deadline.
# update the template with keeping the chars limets, English 159, Amharic 69
We need to remove the amount or money related information from the sms template and replace with other info.
# we need to change the schduler time setting from .env to db
we need to add scheduler time to the db and check the time running the scheduler
by create a setting_scheduler table and add the scheduler time and run from it. 

